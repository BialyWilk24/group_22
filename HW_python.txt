Python. HW_1

1) Создать переменную типа String : a = "Hello_World"
2) Создать переменную типа Integer : b = 24
3) Создать переменную типа Float : c = 24.25654
4) Создать переменную типа Bytes : d = bytes()
5) Создать переменную типа List : e = [1,2,3]
6) Создать переменную типа Tuple : f = 24,25
7) Создать переменную типа Set : g = set()
8. Создать переменную типа Frozen set : h = frozenset()
9) Создать переменную типа Dict : i = {}


10) Вывести в консоль все выше перечисленные переменные с добавлением типа данных. :
	a = 24
	print ("Тип переменной a,  имеющей значение", a, "is", type (a))
	b = 24.25645
	print ("Тип переменной b, имеющей значение", b, "is", type (b))
	c = "Hello_World"
	print ("Тип переменной c, имеющей значение", c, "is", type (c))
	d = bytes()
	print ("Тип переменной d, имеющей значение", d, "is", type (d))
	e = [1,2,3]
		print ("Тип переменной e, имеющей значение", e, "is", type (e))
		f = 24,25
		print ("Тип переменной f, имеющей значение", f, "is", type (f))
		g = set()
		print ("Тип переменной g, имеющей значение", g, "is", type (g))
		h = frozenset()
		print ("Тип переменной h, имеющей значение", h, "is", type (h))
		i = {}
		print ("Тип переменной i, имеющей значение", i, "is", type (i))

11) Создать 2 переменные String, создать переменную в которой суммируете эти переменные. Вывести в консоль. :

		S1= 'Hello'
		S2 = ' World'
		S3= S1 + S2
		print(S3)

12) Создать 2 переменные Integer, создать переменную в которой суммируете эти переменные. Вывести в консоль.

	j = 19
	k = 5
	jk = j + k
	print(jk)

13) Создать переменную в которой Разделите эти переменные Int. Вывести в консоль. :
	
	dif = i / k
	print(dif)

14) Создать переменную в которой Умножите эти переменные Int. Вывести в консоль. :

	mult = i * k
	print(mult)

15) Создать переменную в которой Разделите без остатка эти переменные Int. Вывести в консоль. :

	Delbost = i // k
	print(Delbost)

16) Создать переменную в которой надо присвоить остаток от деления этих переменные Int. Вывести в консоль.

	Delsost = i % k
	print(Delsost)

Выгрузить файл в Git репозиторий.